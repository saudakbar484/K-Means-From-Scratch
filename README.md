# K-Nearest Neighbour Means

This workspace contains a single Jupyter notebook for exploring a K-Nearest Neighbour Means lab.

## Files

- `KNN-Means.ipynb` – the main notebook with analysis, code, and results.

## Usage

1. Open the folder in VS Code or Jupyter Notebook.
2. Launch the notebook: `KNN-Means.ipynb`.
3. Run cells sequentially to reproduce the analysis.

## Notes

- The notebook likely includes data loading, preprocessing, and clustering steps.
- Use a Python environment with common data science libraries installed.
